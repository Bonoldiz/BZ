[
   '{{repeat(50, 0)}}',
   {
     id: '{{index()}}',
     name: '{{lorem(2, "words")}}',
     price: '{{floating(0, 120, 2, "0,0.00")}}',
     
     quantity: '{{integer(1,100)}}',
     employee_id: '{{integer(1,2)}}',
     person_id: '{{integer(1,50)}}',
     location_id: '{{integer(0, 10000)}}',
     description: '{{lorem(1, "paragraphs")}}',
     registration: '{{date(new Date(2018+integer(1,2), integer(1,12), integer(1,30)), new Date(), "YYYY-MM-dd")}}',
     properties: function (tags) {
       var props = [1,2,3];
       var superProps = [5,6,8,9,11,12];
       var selected = [];
       selected.push(props[parseInt(Math.random()*3)]);
       
       superProps.forEach(function(el){
         if(Math.random() > 0.6){
            selected.push(el);
         }
       });
                          
       return selected;
     } 
   }
 ]